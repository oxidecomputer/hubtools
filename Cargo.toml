[package]
name = "hubtools"
version = "0.1.0"
edition = "2021"

[dependencies]
srec = { version = "0.2.0", default-features = false }
anyhow = { version = "1", default-features = false, features = ["std"] }
zip = { version = "0.5" }
